name: test

on:
  push:
    branches:
      - main

jobs:
  Test:
    name: test
    runs-on: Ubuntu-latest
    steps:
      - name: commands
        run: |
          apt update
          apt install clang-format-17